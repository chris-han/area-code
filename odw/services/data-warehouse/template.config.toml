language = "python" # Must be typescript or python
description = "default python project"
post_install_print = """
📂 Go to your project directory:
    $ cd {project_dir}

🥄 Create a virtual environment (optional, recommended):
    $ python3 -m venv .venv
    $ source .venv/bin/activate

📦 Install Dependencies:
    $ uv pip install -r ./requirements.txt

🤖 Set up LLM Integration (optional):
    $ export ANTHROPIC_API_KEY="<your-anthropic-api-key>"
    
    Note: LLM features enable intelligent unstructured data extraction.
    Get your API key at: https://console.anthropic.com

🛠️ Start Moose Server:
    $ moose dev
"""
default_sloan_telemetry="standard"
