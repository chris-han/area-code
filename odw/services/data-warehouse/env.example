# LLM Configuration for Unstructured Data Processing
# Copy this file to .env and update with your actual API keys

# Anthropic API Key for Claude LLM integration
# Get your API key at: https://console.anthropic.com
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Optional: Configure LLM model (defaults to claude-sonnet-4-20250514)
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Optional: Configure LLM temperature for extraction consistency (defaults to 0.1)
# LLM_TEMPERATURE=0.1

# Optional: Configure maximum tokens for LLM responses (defaults to 4000)
# LLM_MAX_TOKENS=4000

# Optional: Enable strict field validation to ensure only requested fields are returned (defaults to true)
# When enabled, the system will filter out any extra fields not explicitly requested in the processing instructions
# LLM_STRICT_FIELD_VALIDATION=true 